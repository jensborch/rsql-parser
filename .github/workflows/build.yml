name: Build

on:
  workflow_call:
  push:
    branches:
      - master
  pull_request:
    types: [opened, reopened]

jobs:
  build:
    uses: jensborch/workflows/.github/gradle-build.yml@main
    secrets: inherit
    with:
      java-version: 8
